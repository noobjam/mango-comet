# Optional RAPIDS acceleration for the GPU VM.
# Install only on the H100/CUDA VM. RAPIDS packaging changes by CUDA/Python
# version, so prefer the RAPIDS install selector when possible:
# https://docs.rapids.ai/install/
#
# Keep this file as a package-name reference, not as a directly installable
# requirements file: CUDA 12 and CUDA 13 packages are mutually exclusive.
# Pin one coherent RAPIDS release in a separate environment.
#
# CUDA 12 example:
#   cuml-cu12==26.6.*
#   cupy-cuda12x>=13.6,<14
#
# CUDA 13 example used by the current H100 VM (CUDA toolkit 13.0):
#   cuml-cu13==26.6.*
#   cupy-cuda13x>=13.6,<14
#
# Add cudf/dask packages only when a measured workload uses them. The current
# motif trainer uses single-GPU cuML HDBSCAN and does not require Dask.
